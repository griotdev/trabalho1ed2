#include "qry.h"

void qry_processar(const char *caminho,
                   HashFile hf_quadras, HashFile hf_habitantes,
                   FILE *svg, FILE *txt) {
    (void)caminho; (void)hf_quadras; (void)hf_habitantes;
    (void)svg; (void)txt; /* stub */
}
